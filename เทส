-- [[ 🐯 TIGER DUMPER PRO V3.2.2 (ULTRA HYBRID EDITION: LITE HUB + ORIGINAL UI FINDER) 🐾 ]]

local fluentSuccess, Fluent = pcall(function()
    return loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
end)

if not fluentSuccess or not Fluent then
    warn("🐯 Tiger Dumper: ไม่สามารถโหลด Fluent UI ได้")
    return
end

local rs = game:GetService("ReplicatedStorage")
local httpService = game:GetService("HttpService")
local uis = game:GetService("UserInputService")
local players = game:GetService("Players")
local lp = players.LocalPlayer
local pGui = lp:WaitForChild("PlayerGui", 5)
local httpRequest = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request

-- ==================== CAPABILITIES CHECK ====================
local Capabilities = {
    writefile = typeof(writefile) == "function",
    makefolder = typeof(makefolder) == "function",
    isfolder = typeof(isfolder) == "function",
    saveinstance = typeof(saveinstance) == "function",
    decompile = typeof(decompile) == "function",
    hookmetamethod = typeof(hookmetamethod) == "function",
    getnamecallmethod = typeof(getnamecallmethod) == "function",
    getrawmetatable = typeof(getrawmetatable) == "function",
    httpRequest = typeof(httpRequest) == "function"
}

local Config = {
    webhookUrl = "",
    spyEnabled = false,
    maxLogs = 150,
    hookInstalled = false
}

local State = {
    remoteLogs = {},
    lastSendTime = {}
}

-- ==================== UTILITIES ====================
local function getInstancePath(obj)
    if not obj then return "nil" end
    local success, path = pcall(function()
        return obj:GetFullName()
    end)
    return success and path or tostring(obj)
end

local function formatValue(v)
    local t = typeof(v)
    if t == "Instance" then
        return string.format("Instance(%s)", v.Name)
    elseif t == "table" then
        local count = 0
        for _ in pairs(v) do count = count + 1 end
        return string.format("table[%d]", count)
    elseif t == "string" then
        return '"' .. v:gsub("\n", "\\n"):sub(1, 30) .. '"'
    elseif t == "number" then
        return string.format("%.2f", v)
    else
        return tostring(v)
    end
end

local function formatArgs(args)
    local parts = {}
    for i, v in ipairs(args) do
        parts[i] = formatValue(v)
    end
    return table.concat(parts, ", ")
end

local function safeWriteFile(name, content)
    if not Capabilities.writefile then return false end
    return pcall(function()
        if Capabilities.makefolder and not isfolder("TigerDumper") then
            makefolder("TigerDumper")
        end
        writefile("TigerDumper/" .. name, content)
    end)
end

-- ==================== DISCORD SENDER ====================
local function sendToDiscord(fileName, fileContent, titleMessage)
    local targetHook = Config.webhookUrl
    if not targetHook or targetHook == "" then 
        return false, "กรุณาระบุ Webhook URL ก่อน" 
    end
    if not Capabilities.httpRequest then 
        return false, "Executor ไม่รองรับ HTTP Request" 
    end

    local maxChunk = 4 * 1024 * 1024
    if #fileContent > maxChunk then
        local totalParts = math.ceil(#fileContent / maxChunk)
        for idx = 1, totalParts do
            local start = (idx - 1) * maxChunk + 1
            local finish = math.min(idx * maxChunk, #fileContent)
            local part = fileContent:sub(start, finish)
            local pName = fileName:gsub("%.%w+$", "") .. "_p" .. idx .. ".txt"
            sendToDiscord(pName, part, string.format("%s [%d/%d]", titleMessage or "Tiger Dumper", idx, totalParts))
            task.wait(1)
        end
        return true
    end

    local boundary = "----TigerBoundary" .. httpService:GenerateGUID(false):gsub("-", "")
    local body = "--" .. boundary .. "\r\n"
        .. 'Content-Disposition: form-data; name="content"\r\n\r\n'
        .. (titleMessage or "📦 Tiger Dumper Export") .. "\r\n"
        .. "--" .. boundary .. "\r\n"
        .. 'Content-Disposition: form-data; name="file"; filename="' .. fileName .. '"\r\n'
        .. "Content-Type: text/plain\r\n\r\n"
        .. fileContent .. "\r\n"
        .. "--" .. boundary .. "--\r\n"

    local ok, res = pcall(function()
        return httpRequest({
            Url = targetHook,
            Method = "POST",
            Headers = { ["Content-Type"] = "multipart/form-data; boundary=" .. boundary },
            Body = body
        })
    end)

    if ok and res and (res.StatusCode == 200 or res.StatusCode == 204) then
        return true
    else
        return false, res and tostring(res.StatusCode) or "ส่งไม่สำเร็จ"
    end
end

-- ==================== SAFE SCANNER ====================
local SAFE_CONTAINERS = {
    workspace,
    rs,
    game:GetService("ReplicatedFirst"),
    lp:WaitForChild("PlayerScripts", 2),
    pGui
}

local function getSafeTargets()
    local list = {}
    for _, service in ipairs(SAFE_CONTAINERS) do
        if service then table.insert(list, service) end
    end
    return list
end

local function scanRemotes(limit)
    limit = limit or 300
    local found = {}
    for _, container in ipairs(getSafeTargets()) do
        for _, obj in ipairs(container:GetDescendants()) do
            if #found >= limit then return found end
            if obj:IsA("RemoteEvent") or obj:IsA("RemoteFunction") or (pcall(function() return obj:IsA("UnreliableRemoteEvent") end) and obj:IsA("UnreliableRemoteEvent")) then
                table.insert(found, { Name = obj.Name, Class = obj.ClassName, Path = getInstancePath(obj) })
            end
        end
    end
    return found
end

local function scanScripts(limit)
    limit = limit or 300
    local found = {}
    for _, container in ipairs(getSafeTargets()) do
        for _, obj in ipairs(container:GetDescendants()) do
            if #found >= limit then return found end
            if obj:IsA("LocalScript") or obj:IsA("ModuleScript") then
                table.insert(found, { Name = obj.Name, Class = obj.ClassName, Path = getInstancePath(obj) })
            end
        end
    end
    return found
end

-- ==================== 5-BUTTON LITE FUNCTIONS ====================
local function f1()
    local s = "=== WORKSPACE TREE ===\n"
    for _, v in ipairs(workspace:GetChildren()) do
        if v:IsA("Folder") or v:IsA("Model") then
            s = s .. string.format("[%s] %s (Children: %d)\n", v.ClassName, v.Name, #v:GetChildren())
        end
    end
    return s
end

local function f2()
    local s, count = "=== TARGETS DUMP ===\n", 0
    local keys = {"egg","drop","plot","spawn","crate","chest","shop","chicken","duck","block","zone"}
    for _, v in ipairs(workspace:GetChildren()) do
        local n = v.Name:lower()
        for _, k in ipairs(keys) do
            if n:find(k, 1, true) then
                local pos = v:IsA("BasePart") and tostring(v.Position) or (v:IsA("Model") and v.PrimaryPart and tostring(v.PrimaryPart.Position)) or "N/A"
                s = s .. string.format("[%s] %s | Pos: %s\nPath: %s\n\n", v.ClassName, v.Name, pos, v:GetFullName())
                count = count + 1; break
            end
        end
        if count >= 60 then break end
    end
    return s
end

local function f3()
    local s = "=== REMOTES LIST ===\n"
    for _, v in ipairs(rs:GetDescendants()) do
        if v:IsA("RemoteEvent") or v:IsA("RemoteFunction") then
            s = s .. string.format("[%s] %s\nPath: %s\n\n", v.ClassName, v.Name, v:GetFullName())
        end
    end
    return s
end

local function f4()
    local s = "=== PLAYER STATS ===\n"
    for _, n in ipairs({"leaderstats","Data","Stats","values"}) do
        local d = lp:FindFirstChild(n)
        if d then
            s = s .. "Folder: " .. n .. "\n"
            for _, v in ipairs(d:GetChildren()) do
                if v:IsA("ValueBase") then s = s .. string.format("  %s = %s\n", v.Name, tostring(v.Value)) end
            end
        end
    end
    return s
end

-- ==================== SPY HOOK ====================
local function installSpyHook()
    if Config.hookInstalled or not Capabilities.hookmetamethod then return end
    local ok, mt = pcall(getrawmetatable, game)
    if not ok or not mt then return end
    
    local oldNamecall
    oldNamecall = hookmetamethod(game, "__namecall", newcclosure(function(self, ...)
        local method = getnamecallmethod()
        if Config.spyEnabled and typeof(self) == "Instance" then
            if method == "FireServer" or method == "InvokeServer" then
                local args = {...}
                task.spawn(function()
                    table.insert(State.remoteLogs, 1, {
                        Time = os.date("%H:%M:%S"),
                        Remote = self.Name,
                        Method = method,
                        Args = formatArgs(args),
                        Path = getInstancePath(self)
                    })
                    if #State.remoteLogs > Config.maxLogs then 
                        table.remove(State.remoteLogs) 
                    end
                end)
            end
        end
        return oldNamecall(self, ...)
    end))
    Config.hookInstalled = true
end

-- ==================== 100% ORIGINAL POP-UP UI FINDER ====================
local textSignalConnection = nil
local activePopupGui = nil

local function OpenPathFinder()
    if activePopupGui and activePopupGui.Parent then
        activePopupGui:Destroy()
        activePopupGui = nil
    end

    if textSignalConnection then
        textSignalConnection:Disconnect()
        textSignalConnection = nil
    end

    local targetParent = pGui
    pcall(function()
        if gethui then targetParent = gethui()
        elseif syn and syn.protect_gui then targetParent = game:GetService("CoreGui") end
    end)

    local sg = Instance.new("ScreenGui")
    sg.Name = "PathDisplayGui"
    sg.ResetOnSpawn = false
    sg.DisplayOrder = 9999
    sg.Parent = targetParent
    activePopupGui = sg

    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, 310, 0, 195)
    frame.Position = UDim2.new(0.5, -155, 0.25, 0)
    frame.BackgroundColor3 = Color3.fromRGB(20, 20, 26)
    frame.BorderSizePixel = 0
    frame.Active = true
    frame.Draggable = true
    frame.Parent = sg

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 10)
    corner.Parent = frame

    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(0, 255, 150)
    stroke.Thickness = 1.5
    stroke.Parent = frame

    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, -30, 0, 25)
    title.Position = UDim2.new(0, 10, 0, 8)
    title.Text = "📋 แตะค้างในช่องเพื่อ Copy Path"
    title.TextColor3 = Color3.fromRGB(0, 255, 150)
    title.TextSize = 12
    title.Font = Enum.Font.GothamBold
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.BackgroundTransparency = 1
    title.Parent = frame

    local closeBtn = Instance.new("TextButton")
    closeBtn.Size = UDim2.new(0, 25, 0, 25)
    closeBtn.Position = UDim2.new(1, -30, 0, 5)
    closeBtn.Text = "✕"
    closeBtn.TextColor3 = Color3.fromRGB(255, 90, 90)
    closeBtn.TextSize = 14
    closeBtn.BackgroundTransparency = 1
    closeBtn.Parent = frame
    closeBtn.MouseButton1Click:Connect(function()
        if textSignalConnection then
            textSignalConnection:Disconnect()
            textSignalConnection = nil
        end
        sg:Destroy()
        activePopupGui = nil
    end)

    local searchBox = Instance.new("TextBox")
    searchBox.Size = UDim2.new(1, -20, 0, 32)
    searchBox.Position = UDim2.new(0, 10, 0, 35)
    searchBox.PlaceholderText = "พิมพ์ชื่อ UI ที่ต้องการหา (เช่น Multiplier, Egg)..."
    searchBox.Text = ""
    searchBox.TextColor3 = Color3.new(1, 1, 1)
    searchBox.BackgroundColor3 = Color3.fromRGB(35, 40, 50)
    searchBox.TextSize = 11
    searchBox.Font = Enum.Font.Gotham
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = frame
    Instance.new("UICorner", searchBox).CornerRadius = UDim.new(0, 6)

    local pathBox = Instance.new("TextBox")
    pathBox.Size = UDim2.new(1, -20, 0, 45)
    pathBox.Position = UDim2.new(0, 10, 0, 72)
    pathBox.Text = "พิมพ์ชื่อแล้วกดปุ่มสแกนด้านล่าง..."
    pathBox.TextColor3 = Color3.new(1, 1, 1)
    pathBox.BackgroundColor3 = Color3.fromRGB(32, 32, 40)
    pathBox.TextSize = 11
    pathBox.Font = Enum.Font.Code
    pathBox.ClearTextOnFocus = false
    pathBox.TextWrapped = true
    pathBox.Parent = frame
    Instance.new("UICorner", pathBox).CornerRadius = UDim.new(0, 6)

    local liveLabel = Instance.new("TextLabel")
    liveLabel.Size = UDim2.new(1, -20, 0, 30)
    liveLabel.Position = UDim2.new(0, 10, 0, 122)
    liveLabel.Text = "สถานะ: พร้อมค้นหา..."
    liveLabel.TextColor3 = Color3.fromRGB(200, 200, 210)
    liveLabel.TextSize = 11
    liveLabel.Font = Enum.Font.Gotham
    liveLabel.TextWrapped = true
    liveLabel.BackgroundTransparency = 1
    liveLabel.Parent = frame

    local refreshBtn = Instance.new("TextButton")
    refreshBtn.Size = UDim2.new(1, -20, 0, 30)
    refreshBtn.Position = UDim2.new(0, 10, 0, 156)
    refreshBtn.Text = "🔄 สแกนค้นหาตามชื่อที่พิมพ์"
    refreshBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 100)
    refreshBtn.TextColor3 = Color3.new(1, 1, 1)
    refreshBtn.Font = Enum.Font.GothamBold
    refreshBtn.TextSize = 12
    refreshBtn.Parent = frame
    Instance.new("UICorner", refreshBtn).CornerRadius = UDim.new(0, 6)

    local function scanTargetUI()
        if textSignalConnection then
            textSignalConnection:Disconnect()
            textSignalConnection = nil
        end

        local keyword = searchBox.Text:lower()
        if keyword == "" then
            keyword = "multiplier"
        end

        local target = nil
        local searchScopes = {pGui, workspace}
        for _, rootObj in ipairs(searchScopes) do
            if rootObj then
                for _, obj in ipairs(rootObj:GetDescendants()) do
                    if (obj:IsA("TextLabel") or obj:IsA("TextButton")) and not obj:IsDescendantOf(sg) then
                        local text = tostring(obj.Text):lower()
                        local name = tostring(obj.Name):lower()
                        if string.find(text, keyword, 1, true) or string.find(name, keyword, 1, true) then
                            target = obj
                            break
                        end
                    end
                end
            end
            if target then break end
        end

        if target then
            pathBox.Text = target:GetFullName()
            liveLabel.Text = "🎯 เจอเป้าหมาย: " .. tostring(target.Text)
            textSignalConnection = target:GetPropertyChangedSignal("Text"):Connect(function()
                liveLabel.Text = "🔥 ข้อความเปลี่ยนสด: " .. tostring(target.Text)
            end)
        else
            pathBox.Text = "ไม่พบ UI ที่ตรงกับคำว่า: " .. searchBox.Text
            liveLabel.Text = "❌ ค้นหาไม่พบ ลองเปลี่ยนคำค้นหา"
        end
    end

    refreshBtn.MouseButton1Click:Connect(scanTargetUI)
end

-- ==================== UI SETUP (FLUENT) ====================
local Window = Fluent:CreateWindow({
    Title = "🐯 Tiger Dumper Pro",
    SubTitle = "v3.2.2 Ultimate Hub",
    TabWidth = 125,
    Size = UDim2.fromOffset(500, 360),
    Acrylic = false,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

local Tabs = {
    Scanner = Window:AddTab({ Title = "📡 Scanner" }),
    Spy = Window:AddTab({ Title = "🕵️ Spy" }),
    Dumper = Window:AddTab({ Title = "💾 Dumper" }),
    LiteHub = Window:AddTab({ Title = "⚡ Lite Hub" }),
    UIFinder = Window:AddTab({ Title = "🎯 UI Finder" }),
    Status = Window:AddTab({ Title = "⚙️ Status" })
}

-- TAB 1: SCANNER
Tabs.Scanner:AddButton({
    Title = "🔍 สแกน Remote และบันทึก",
    Callback = function()
        local remotes = scanRemotes(300)
        local output = string.format("🔍 Remote List (Total: %d)\n\n", #remotes)
        for _, r in ipairs(remotes) do
            output = output .. string.format("[%s] %s\n└─ %s\n", r.Class, r.Name, r.Path)
        end
        safeWriteFile("RemoteList.txt", output)
        Tabs.Scanner:AddParagraph({
            Title = "สแกนสำเร็จ",
            Content = string.format("พบ %d รายการ (บันทึกลง TigerDumper/RemoteList.txt แล้ว)", #remotes)
        })
    end
})

-- TAB 2: SPY
Tabs.Spy:AddToggle("EnableSpy", { Title = "🕵️ เปิด Remote Spy", Default = false }):OnChanged(function(v)
    Config.spyEnabled = v
    if v then installSpyHook() end
end)

Tabs.Spy:AddButton({
    Title = "🚀 ส่ง Remote Logs เข้า Discord",
    Callback = function()
        local text = "=== SPY LOGS ===\n\n"
        for _, log in ipairs(State.remoteLogs) do
            text = text .. string.format("[%s] %s:%s(%s)\nPath: %s\n\n", log.Time, log.Remote, log.Method, log.Args, log.Path)
        end
        local ok, err = sendToDiscord("SpyLogs.txt", text, "🕵️ Remote Logs")
        Window:Dialog({
            Title = ok and "✅ สำเร็จ" or "❌ ล้มเหลว",
            Content = ok and "ส่ง Logs เข้า Discord เรียบร้อย" or tostring(err),
            Buttons = {{ Title = "ตกลง" }}
        })
    end
})

-- TAB 3: DUMPER
Tabs.Dumper:AddInput("DiscordWebhook", {
    Title = "🔗 Webhook URL (แชร์ร่วมกันทุกแท็บ)",
    Default = "",
    Placeholder = "https://discord.com/api/webhooks/...",
    Callback = function(v) Config.webhookUrl = v end
})

Tabs.Dumper:AddButton({
    Title = "⚡ Turbo Decompile (Script ที่มองเห็น)",
    Description = "แกะโค้ด Local/ModuleScript แล้วส่ง Discord ทันที",
    Callback = function()
        if not Capabilities.decompile then
            Window:Dialog({ Title = "แจ้งเตือน", Content = "Executor นี้ไม่รองรับ decompile()", Buttons = {{ Title = "ตกลง" }} })
            return
        end

        if State.lastSendTime["Turbo"] and tick() - State.lastSendTime["Turbo"] < 10 then return end
        State.lastSendTime["Turbo"] = tick()

        task.spawn(function()
            local scripts = scanScripts(150)
            local dump = "-- TIGER DUMP ENGINE\n\n"
            local count = 0

            for _, target in ipairs(scripts) do
                local inst = game:FindFirstChild(target.Path, true) or rs:FindFirstChild(target.Name, true)
                if inst and (inst:IsA("LocalScript") or inst:IsA("ModuleScript")) then
                    local ok, src = pcall(decompile, inst)
                    if ok and typeof(src) == "string" and #src > 0 then
                        dump = dump .. "\n\n-- [[" .. target.Path .. "]]\n" .. src
                        count = count + 1
                    end
                end
                task.wait(0.02)
            end

            local ok, err = sendToDiscord("DumpedScripts.lua", dump, string.format("⚡ แกะสำเร็จ %d สคริปต์", count))
            Window:Dialog({
                Title = ok and "✅ สำเร็จ" or "❌ ล้มเหลว",
                Content = ok and string.format("ส่งโค้ด %d ไฟล์เรียบร้อย", count) or tostring(err),
                Buttons = {{ Title = "ตกลง" }}
            })
        end)
    end
})

Tabs.Dumper:AddButton({
    Title = "💾 SaveInstance (.rbxl)",
    Callback = function()
        if Capabilities.saveinstance then
            task.spawn(function()
                pcall(saveinstance, { mode = "optimized", noscripts = false })
            end)
        else
            Window:Dialog({ Title = "แจ้งเตือน", Content = "ไม่รองรับ saveinstance()", Buttons = {{ Title = "ตกลง" }} })
        end
    end
})

-- TAB 4: LITE HUB (เมนูดัมพ์ด่วน 5 ปุ่ม)
Tabs.LiteHub:AddParagraph({
    Title = "⚡ เมนูดัมพ์ด่วน (Lite Hub)",
    Content = "กดปุ่มเพื่อส่งข้อมูลแยกหมวดหมู่เข้า Discord ทันทีโดยใช้ Webhook URL เดียวกัน"
})

Tabs.LiteHub:AddButton({
    Title = "1. 🌳 Dump Workspace Tree",
    Callback = function() sendToDiscord("Workspace.txt", f1(), "🌳 Workspace Tree") end
})

Tabs.LiteHub:AddButton({
    Title = "2. 🎯 Dump
