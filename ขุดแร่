-- [[ LITTLE TIGER HUB - V7.5 (SMART BUY REWRITTEN) ]] --

local oldUI = game:GetService("CoreGui"):FindFirstChild("TigerHubUI") or game:GetService("Players").LocalPlayer:FindFirstChildOfClass("PlayerGui"):FindFirstChild("TigerHubUI")
if oldUI then oldUI:Destroy() end

-- ตั้งค่าสถานะเปิด/ปิดเริ่มต้น
_G.AutoLootToggle = false
_G.AutoMineToggle = false
_G.AutoDepositToggle = false
_G.AutoCollectToggle = false
_G.SmartAutoBuyToggle = false
_G.AutoMergeToggle = false
_G.AutoUpgradeToggle = false

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- ==========================================
-- 1. สร้างหน้าตา UI (GRID STYLE - 7 SLOTS)
-- ==========================================
local TigerHubUI = Instance.new("ScreenGui")
TigerHubUI.Name = "TigerHubUI"
TigerHubUI.ResetOnSpawn = false
pcall(function() TigerHubUI.Parent = game:GetService("CoreGui") end)
if not TigerHubUI.Parent then TigerHubUI.Parent = LocalPlayer:FindFirstChildOfClass("PlayerGui") end

local MainFrame = Instance.new("Frame")
MainFrame.Parent = TigerHubUI
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 17)
MainFrame.Position = UDim2.new(0.35, 0, 0.25, 0)
MainFrame.Size = UDim2.new(0, 380, 0, 250)
MainFrame.Active = true
MainFrame.BorderSizePixel = 0

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 10)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Parent = MainFrame
MainStroke.Color = Color3.fromRGB(235, 94, 40)
MainStroke.Thickness = 1.2
MainStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

local TopBar = Instance.new("Frame")
TopBar.Parent = MainFrame
TopBar.BackgroundColor3 = Color3.fromRGB(24, 24, 26)
TopBar.Size = UDim2.new(1, 0, 0, 35)
TopBar.BorderSizePixel = 0

local TopBarCorner = Instance.new("UICorner")
TopBarCorner.CornerRadius = UDim.new(0, 10)
TopBarCorner.Parent = TopBar

local Divider = Instance.new("Frame")
Divider.Parent = TopBar
Divider.BackgroundColor3 = Color3.fromRGB(235, 94, 40)
Divider.Position = UDim2.new(0, 0, 1, -1)
Divider.Size = UDim2.new(1, 0, 0, 1)
Divider.BorderSizePixel = 0

local Title = Instance.new("TextLabel")
Title.Parent = TopBar
Title.BackgroundTransparency = 1
Title.Position = UDim2.new(0, 14, 0, 0)
Title.Size = UDim2.new(0, 250, 1, -1)
Title.Font = Enum.Font.GothamBold
Title.Text = "🐯 TIGER HUB <font color='#EB5E28'>v7.5</font>"
Title.TextColor3 = Color3.fromRGB(240, 240, 240)
Title.TextSize = 13
Title.RichText = true
Title.TextXAlignment = Enum.TextXAlignment.Left

local CloseBtn = Instance.new("TextButton")
CloseBtn.Parent = TopBar
CloseBtn.BackgroundTransparency = 1
CloseBtn.Position = UDim2.new(1, -35, 0, 0)
CloseBtn.Size = UDim2.new(0, 35, 1, -1)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Text = "✕"
CloseBtn.TextColor3 = Color3.fromRGB(120, 120, 120)
CloseBtn.TextSize = 13

-- ระบบลากหน้าต่าง
local dragging, dragInput, dragStart, startPos
TopBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragging = false end
        end)
    end
end)
TopBar.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Parent = TigerHubUI
ToggleBtn.BackgroundColor3 = Color3.fromRGB(15, 15, 17)
ToggleBtn.Position = UDim2.new(0, 15, 0.5, -20)
ToggleBtn.Size = UDim2.new(0, 90, 0, 35)
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.Text = "🐯 OPEN"
ToggleBtn.TextColor3 = Color3.fromRGB(235, 94, 40)
ToggleBtn.TextSize = 12
ToggleBtn.Visible = false

local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(0, 6)
ToggleCorner.Parent = ToggleBtn

local ToggleStroke = Instance.new("UIStroke")
ToggleStroke.Parent = ToggleBtn
ToggleStroke.Color = Color3.fromRGB(235, 94, 40)
ToggleStroke.Thickness = 1

local Content = Instance.new("Frame")
Content.Parent = MainFrame
Content.BackgroundTransparency = 1
Content.Position = UDim2.new(0, 15, 0, 48)
Content.Size = UDim2.new(1, -30, 1, -55)

local UIGridLayout = Instance.new("UIGridLayout")
UIGridLayout.Parent = Content
UIGridLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIGridLayout.CellSize = UDim2.new(0, 170, 0, 34)
UIGridLayout.CellPad
